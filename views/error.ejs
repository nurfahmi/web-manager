<% const extraScripts = ''; %>
<%- include('partials/header') %>

<div class="min-h-[60vh] flex flex-col items-center justify-center">
  <div class="w-20 h-20 bg-red-100 dark:bg-red-900/30 rounded-full flex items-center justify-center mb-6">
    <i class="bi bi-exclamation-triangle text-red-500 text-3xl"></i>
  </div>
  <h1 class="text-3xl font-bold mb-2"><%= title || 'Error' %></h1>
  <p class="text-gray-500 dark:text-gray-400 text-lg mb-8"><%= message || 'Something went wrong.' %></p>
  <a href="/admin" class="px-6 py-3 bg-brand-600 hover:bg-brand-700 text-white font-semibold rounded-xl transition-colors">
    <i class="bi bi-arrow-left mr-2"></i>Back to Dashboard
  </a>
</div>

<%- include('partials/footer') %>
