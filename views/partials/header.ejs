<!DOCTYPE html>
<html lang="en" class="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || 'Indosofthouse App Panel' %></title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      darkMode: 'class',
      theme: {
        extend: {
          colors: {
            brand: {
              50: '#eff6ff', 100: '#dbeafe', 200: '#bfdbfe', 300: '#93c5fd',
              400: '#60a5fa', 500: '#3b82f6', 600: '#2563eb', 700: '#1d4ed8',
              800: '#1e40af', 900: '#1e3a8a', 950: '#172554'
            }
          }
        }
      }
    }
  </script>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
  <script>
    if (localStorage.getItem('theme') === 'light') {
      document.documentElement.classList.remove('dark');
    }
  </script>
</head>
<body class="bg-gray-50 text-gray-900 dark:bg-gray-950 dark:text-gray-100 min-h-screen transition-colors duration-300">

<% if (typeof user !== 'undefined' && user) { %>
<nav class="bg-white dark:bg-gray-900 border-b border-gray-200 dark:border-gray-800 sticky top-0 z-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16">
      <div class="flex items-center gap-3">
        <div class="w-8 h-8 bg-brand-600 rounded-lg flex items-center justify-center">
          <i class="bi bi-grid-fill text-white text-sm"></i>
        </div>
        <a href="/admin" class="text-lg font-bold text-brand-600 dark:text-brand-400">Indosofthouse App Panel</a>
      </div>
      <div class="flex items-center gap-4">
        <% if (user.role === 'SUPER_ADMIN') { %>
        <a href="/admin/terminal" class="p-2 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-800 transition-colors text-gray-500 dark:text-gray-400" title="Terminal">
          <i class="bi bi-terminal"></i>
        </a>
        <a href="/admin/files" class="p-2 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-800 transition-colors text-gray-500 dark:text-gray-400" title="File Manager">
          <i class="bi bi-folder"></i>
        </a>
        <a href="/admin/settings" class="p-2 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-800 transition-colors text-gray-500 dark:text-gray-400" title="Settings">
          <i class="bi bi-gear"></i>
        </a>
        <% } %>
        <button onclick="toggleTheme()" class="p-2 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-800 transition-colors" title="Toggle theme">
          <i class="bi bi-sun-fill text-gray-500 dark:text-gray-400 dark:hidden"></i>
          <i class="bi bi-moon-fill text-gray-400 hidden dark:inline"></i>
        </button>
        <div class="flex items-center gap-2 text-sm">
          <span class="hidden sm:inline text-gray-600 dark:text-gray-400"><%= user.username %></span>
          <span class="px-2 py-0.5 text-xs rounded-full bg-brand-100 text-brand-700 dark:bg-brand-900 dark:text-brand-300 font-medium"><%= user.role %></span>
        </div>
        <form action="/logout" method="GET" class="inline">
          <input type="hidden" name="_csrf" value="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">
          <button type="submit" class="text-sm text-gray-500 hover:text-red-500 dark:text-gray-400 dark:hover:text-red-400 transition-colors">
            <i class="bi bi-box-arrow-right"></i> Logout
          </button>
        </form>
      </div>
    </div>
  </div>
</nav>
<% } %>

<main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
